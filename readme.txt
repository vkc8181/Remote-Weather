Step 1: Install dependencies by using the command: "npm i"

Step 2: Use to run the application, use the command: "npm start"

Step 3: Once the application starts running, visit "localhost:3000" in browser.